[SERVICE]
    Flush        5
    Daemon       Off
    Log_Level    debug
    Log_File     /dev/stdout
    Parsers_File parsers.conf

[INPUT]
    Name Forward
    Port 24224

[INPUT]
    Name cpu

[INPUT]
    Name tail
    Tag nginx
    path /test.log
    path_key test.log
    db  /nginx.pos
    Parser plain

[FILTER]
    Name record_modifier
    Match *
    Record server_name ${HOSTNAME}
    Record category Tracelog
    Record app_name docker-hsdp-out

[OUTPUT]
    Name  stdout
    Match *

[Output]
    Name hsdp
    Match *
    IngestorHost https://logingestor-int2.us-east.philips-healthsuite.com
    SharedKey foo
    SecretKey bar
    ProductKey baz
